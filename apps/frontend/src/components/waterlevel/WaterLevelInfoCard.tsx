// This file has been replaced by shared/WaterLevelInfoCard.tsx
// We're using this as a proxy to maintain compatibility with existing imports
export { WaterLevelInfoCard } from '../shared/WaterLevelInfoCard';

// No longer needed - the actual implementation is in the shared component
// The proxy file only needs to re-export the component 